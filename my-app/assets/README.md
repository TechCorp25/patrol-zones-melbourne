# Placeholder asset

`icon.png` is a **text placeholder only** in this branch because binary files are not supported for PR creation in this environment.
Replace `my-app/assets/icon.png` with a real PNG production app icon before release builds.
